const stackName = `eventnet-test-stack`;
const dbname = `${stackName}-ddbtable`;

describe("Basic Test > ", () => {
  test("Add data to Dynamo Table, capture outcome", async () => {
    // add into dynamo
    // capture result via EventNet
  });
});
